/**
 * Main class for running the contacts application. 
 * ..
 * 
 * @author Patrick Palmroos
 * @param
 * @return
 */
class ContactsApp {
    public static void main(String [] args) {
        GUI.printContaints(FileTranslator.fileToString());
    }
}